# 啟動腳本問題排查指南

## 問題：執行後視窗立即消失

這表示腳本遇到錯誤後立即退出了。

## 解決方法

### 方法 1：使用偵錯版腳本（推薦）

我已經建立了一個偵錯版本的啟動腳本：

**執行：** `start_website_DEBUG.bat`

這個版本會：
- ✅ 顯示每個檢查步驟
- ✅ 顯示詳細的錯誤訊息
- ✅ 遇到錯誤時不會關閉視窗
- ✅ 顯示當前目錄路徑

### 方法 2：手動檢查

開啟命令提示字元（cmd），然後依序執行：

```batch
# 1. 切換到專案目錄
cd /d d:\tea-website

# 2. 檢查 Node.js
node --version

# 3. 檢查 npm
npm --version

# 4. 檢查檔案
dir package.json

# 5. 安裝依賴（如果需要）
npm install

# 6. 啟動開發伺服器
npm run dev
```

### 方法 3：從命令提示字元執行

1. 按 `Win + R` 開啟執行視窗
2. 輸入 `cmd` 按 Enter
3. 在命令提示字元中輸入：
   ```
   cd /d d:\tea-website
   start_website_DEBUG.bat
   ```

這樣即使腳本出錯，命令提示字元視窗也不會關閉。

## 常見錯誤原因

### 1. Node.js 未正確安裝
**症狀：** 找不到 node 或 npm
**解決：** 
- 重新安裝 Node.js
- 確保勾選「Add to PATH」
- 安裝後重新啟動電腦

### 2. 不在正確的目錄
**症狀：** 找不到 package.json
**解決：**
- 確認腳本在 `d:\tea-website` 目錄中
- 確認 package.json 存在

### 3. 網路問題
**症狀：** npm install 失敗
**解決：**
- 檢查網路連線
- 嘗試使用手機熱點
- 稍後再試

### 4. 權限問題
**症狀：** 無法寫入檔案
**解決：**
- 以系統管理員身分執行
- 檢查防毒軟體設定

## 下一步

請執行 `start_website_DEBUG.bat`，然後告訴我您看到什麼錯誤訊息，我會幫您解決！
