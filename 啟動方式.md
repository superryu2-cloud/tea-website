# 茶葉網站啟動方式總結

## ✅ 確認可用的方式

### 手動啟動（最可靠）

1. 開啟命令提示字元或 PowerShell
2. 執行：
   ```
   cd /d d:\tea-website
   npm run dev
   ```
3. 開啟瀏覽器：`http://localhost:5173`

---

## 🔧 批次檔啟動

### 新版 start_website.bat

我已經更新了 `start_website.bat`，現在它會：
- 在新的命令視窗中啟動伺服器
- 自動開啟瀏覽器
- 視窗不會閃退

**使用方式：**
雙擊 `start_website.bat`

---

## 📝 快速參考

### 啟動網站
```
cd /d d:\tea-website
npm run dev
```

### 停止伺服器
在命令視窗中按 `Ctrl + C`，然後按 `Y`

### 網址
http://localhost:5173

---

## 💡 建議

由於批次檔在您的系統上可能有相容性問題，建議：

1. **日常使用**：使用手動啟動方式
2. **快速啟動**：試試新版的 `start_website.bat`
3. **建立捷徑**：可以建立一個桌面捷徑，指向命令：
   ```
   cmd /k "cd /d d:\tea-website && npm run dev"
   ```

這樣就不需要每次都手動輸入指令了！
