export const UI_FLAGS = {
  // When true: pages show a "筆記卡 + 展開百科" experience (atlas on demand).
  // When false (default): pages open the full content directly (no notes card).
  notesMode: false,

  // When true: uses the older "filter + cards" varieties page (kept for comparison).
  // When false (default): uses 7 subpages + left switcher (綠/黃/白/青/紅/黑/普洱).
  legacyVarieties: false,
};
