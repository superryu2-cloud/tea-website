# 手動啟動網站指南

由於批次檔可能有相容性問題，請使用以下手動方式啟動網站：

## 方法 1：使用命令提示字元（推薦）

1. **開啟命令提示字元**
   - 按 `Win + R`
   - 輸入 `cmd`
   - 按 Enter

2. **切換到專案目錄**
   ```
   cd /d d:\tea-website
   ```

3. **檢查 Node.js**
   ```
   node --version
   ```
   應該顯示版本號（例如：v20.x.x）

4. **安裝依賴（首次執行）**
   ```
   npm install
   ```
   等待安裝完成（可能需要 2-5 分鐘）

5. **啟動開發伺服器**
   ```
   npm run dev
   ```

6. **開啟瀏覽器**
   - 等待看到 "Local: http://localhost:5173/" 訊息
   - 開啟瀏覽器，輸入網址：`http://localhost:5173`

## 方法 2：使用 PowerShell

1. **開啟 PowerShell**
   - 在專案資料夾 `d:\tea-website` 上按右鍵
   - 選擇「在終端機中開啟」或「Open PowerShell window here」

2. **執行指令**
   ```powershell
   npm run dev
   ```

3. **開啟瀏覽器**
   - 瀏覽器輸入：`http://localhost:5173`

## 方法 3：使用極簡版腳本

我建立了一個極簡版的啟動腳本：

**執行：** `START.bat`

如果這個也閃退，請使用方法 1 手動啟動。

## 停止伺服器

在命令提示字元視窗中按 `Ctrl + C`，然後按 `Y` 確認。

## 常見問題

### Q: 顯示「找不到 node」
**A:** Node.js 未正確安裝或未加入 PATH
- 重新安裝 Node.js
- 安裝時確保勾選「Add to PATH」
- 重新啟動電腦

### Q: npm install 很慢或失敗
**A:** 網路問題
- 檢查網路連線
- 嘗試使用手機熱點
- 稍後再試

### Q: 埠號 5173 被佔用
**A:** 修改埠號
- 編輯 `package.json`
- 或執行：`npm run dev -- --port 3000`

## 成功啟動的標誌

當您看到類似以下訊息時，表示成功：

```
VITE v5.x.x  ready in xxx ms

➜  Local:   http://localhost:5173/
➜  Network: use --host to expose
```

此時開啟瀏覽器輸入 `http://localhost:5173` 即可看到網站！
